{
  "name": "db",
  "scripts": {
    "db:studio": "prisma studio",
    "db:push": "prisma db push --skip-generate",
    "dev": "prisma generate && prisma db push",
    "postinstall": "prisma generate"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "prisma": "6.1.0"
  },
  "dependencies": {
    "@prisma/adapter-mariadb": "^6.11.1",
    "@prisma/client": "6.1.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
